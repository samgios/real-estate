import { defineSchema } from "convex/server"

/**
 * Schema for the Italy real estate decision tools app.
 * Add tables with defineTable() as needed (e.g. saved regions, notes).
 */
export default defineSchema({})
